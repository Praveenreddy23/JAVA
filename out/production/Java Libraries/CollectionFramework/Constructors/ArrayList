--@ArrayList:
-->In Arraylist class the constructor is overloaded
    1.Arraylist constructor without parameter. [ Arraylist ( ) ]
       -> This constructor will be invoked .
       -> whenever an object is created which will initialize the default capacity as 10.
    2. Arraylist ( int initial capacity ) :
       -> it will initialize the capacity as per the user input .
    3. Arraylist ( collection c ) :
       -> it helps to copy from one collection to another collections object.

       1.ArrayList a = new ArrayList();
       2.ArrayList b = new ArrayList(60);
