--@HashSet And LinkedHashSet:
  1.No arg Constructor:(HashSet();LinkedHashSet();)
     HashSet h = new HashSet();
     LinkedHashSet h = new LinkedHashSet();
  2.Two arg Constructor:(HashSet(n,f);new LinkedHashSet(n,f);)
      HashSet h = new HashSet(n,f);
      LinkedHashSet h = new LinkedHashSet(n,f);
      n--> int type , Specifies initial capacity
      f--> float type , specifies load factor.
  3.Collection type arg Constructor;(HashSet(Collection c);)
